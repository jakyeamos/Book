import { ReaderAccountStub } from "@/components/reader/ReaderAccountStub";

export const dynamic = "force-dynamic";

export default function ReaderAccountPage(): React.ReactElement {
  return <ReaderAccountStub title="Your reading life" description="Highlights, notes, reading progress, and listening preferences will appear here once you sign in." />;
}
