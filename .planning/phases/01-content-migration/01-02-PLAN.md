---
phase: 01-content-migration
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - chapters/DISCREPANCY.md
  - chapters/ch01.html
  - chapters/ch02.html
autonomous: false
requirements:
  - CONT-01
  - CONT-02
  - CONT-03

must_haves:
  truths:
    - "chapters/DISCREPANCY.md exists and is non-empty with a structured comparison of Ch1 and Ch2 HTML vs docx versions"
    - "chapters/ch01.html exists as a valid chapter fragment with the full docx text of The Ritual (per user decision on canonical version)"
    - "chapters/ch02.html exists as a valid chapter fragment with the full docx text of The Routine, including the additional docx content not in the current HTML"
    - "User has reviewed the DISCREPANCY.md report and confirmed which version is canonical for Ch1 before ch01.html is finalized"
  artifacts:
    - path: "chapters/DISCREPANCY.md"
      provides: "Side-by-side comparison of HTML vs docx for Ch1 and Ch2 — flagging all divergences for author review"
    - path: "chapters/ch01.html"
      provides: "Chapter 1 fragment with correct content, ch1-s* section IDs, formatting classes applied"
      contains: "chapter1"
    - path: "chapters/ch02.html"
      provides: "Chapter 2 fragment with correct content including docx-only ending, ch2-s* section IDs"
      contains: "chapter2"
  key_links:
    - from: "chapters/ch01.html"
      to: "chapters/raw/ch01-raw.html"
      via: "Human-assisted formatting pass over mammoth output"
      pattern: "class=\"chapter\" id=\"chapter1\""
    - from: "chapters/ch02.html"
      to: "chapters/raw/ch02-raw.html"
      via: "Human-assisted formatting pass plus docx additions"
      pattern: "class=\"chapter\" id=\"chapter2\""
---

<objective>
Produce a DISCREPANCY.md comparison report for Chapters 1 and 2, then — after user review — author the final ch01.html and ch02.html fragments from the docx sources using mammoth's raw output as a base.

Purpose: Chapters 1 and 2 have significant, documented divergences between the current index.html and the docx files. The research found Ch1 HTML (~3,189 words) is far richer than the Ch1 docx (~1,761 words); Ch2 docx (~2,650 words) is longer than the current HTML (~1,977 words) and lacks the Harley character name entirely. A human must decide which version is canonical for Ch1 before any HTML is finalized.
Output: chapters/DISCREPANCY.md, chapters/ch01.html, chapters/ch02.html
</objective>

<execution_context>
@C:/Users/gspea/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gspea/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-content-migration/01-CONTEXT.md
@.planning/phases/01-content-migration/01-RESEARCH.md
@.planning/phases/01-content-migration/01-01-SUMMARY.md
</context>

<interfaces>
<!-- Contracts from Plan 01 and from existing codebase -->

From chapters/raw/ (created by Plan 01):
  chapters/raw/ch01-raw.html   — mammoth output of The ritual + the rehersal(1D).docx
  chapters/raw/ch02-raw.html   — mammoth output of The routine(2D).docx

Chapter fragment format (from RESEARCH.md Pattern 1):
```html
<div class="chapter" id="chapter1">
  <section id="ch1-s1" class="book-section">
    <p class="date-location"><em>Sunday, March 8th, 2036<br>Olympia, Washington</em></p>
    <h2 class="chapter-title"><strong>THE RITUAL.</strong></h2>
    <!-- prose -->
  </section>
  <section id="ch1-s2" class="book-section"><!-- continuation --></section>
  <section id="ch1-s3" class="book-section"><!-- poem section --></section>
</div>
```

CSS classes available in styles.css (do not recreate):
  .poetic-text, .bold-grow-1, .bold-grow-2, .bold-grow-3, .bold-grow-4
  .centered-text, .right-aligned, .staggered-indent, .date-location
  .chapter-title, .harley, .alegreya-sans-sc-regular, .alegreya-sans-sc-medium

Section ID convention: ch1-s1, ch1-s2, ch1-s3 (per-chapter prefix)
  Do NOT use: section1, section2, section3 (reserved for existing audio map in script.js)

From existing index.html (content to be MOVED, not recreated):
  Ch1: <div class="chapter" id="chapter1"> at line 28 — includes bold-grow SAME sequence, Sisyphus/hairpin scene, Nico meeting
  Ch2: <div class="chapter" id="chapter2"> at line 293 — includes Harley character with .harley class
  Note: These will be REMOVED from index.html in Plan 05

Formatting signals from docx (RESEARCH.md Code Examples):
  date-location: mammoth outputs as plain <p> — wrap manually: <p class="date-location"><em>...</em></p>
  staggered-indent: mammoth outputs as sequence of <p> — wrap in <div class="staggered-indent">
  poetic lines: mammoth outputs poem lines as <p> after an <h3> — wrap in <div class="poetic-text">
  bold-grow pattern: NOT in Ch1 docx (docx uses lowercase "same") — exists only in current HTML

CRITICAL pitfalls for Ch1 and Ch2:
  - Ch1 docx ends at library entrance; HTML has Sisyphus/hairpin/Nico-meeting content NOT in docx
  - Ch1 docx "The rehearsal" is a poem subsection within Ch1 (not a separate chapter)
  - Ch2 docx has NO Harley name — anonymous italic commands only ("You're already clean")
  - Ch2 docx has ~673 more words than current HTML — includes full Gianna ending + "The Ever-Absent Mercy of a Clock" poem
  - Ch2 current HTML has inline style= attributes (e.g., style="font-size: 1.05em") — these must be replaced with .bold-grow-* classes in the fragment
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Produce DISCREPANCY.md comparison report</name>
  <files>chapters/DISCREPANCY.md</files>
  <action>
    Read the raw mammoth outputs from `chapters/raw/ch01-raw.html` and `chapters/raw/ch02-raw.html`. Also read the current Ch1 and Ch2 content from `index.html` (Chapter 1 at `<div class="chapter" id="chapter1">`, Chapter 2 at `<div class="chapter" id="chapter2">`).

    Produce `chapters/DISCREPANCY.md` with the following structure:

    ```markdown
    # Content Discrepancy Report — Chapters 1 & 2

    **Produced:** [date]
    **Purpose:** Author review required before finalizing ch01.html and ch02.html

    ---

    ## Chapter 1: The Ritual

    ### Word Count Comparison
    | Source | Approximate Words |
    |--------|-------------------|
    | Docx (mammoth raw) | ~1,761 |
    | Current index.html | ~3,189 |

    ### Content in HTML but NOT in Docx
    List every scene/passage/element present in index.html Ch1 but absent from the docx:
    - [scene name]: [brief description and approximate location]
    ...

    ### Content in Docx but NOT in HTML
    List everything in the docx not yet in the HTML:
    ...

    ### Formatting Differences
    - bold-grow SAME sequence: [HTML has it, docx does not — confirm which is canonical]
    - ...

    ### Decision Required
    [ ] Use docx as canonical (HTML content above will be lost)
    [ ] Use HTML as canonical (docx content will be discarded)
    [ ] Merge (specify merge strategy)

    ---

    ## Chapter 2: The Routine

    ### Word Count Comparison
    | Source | Approximate Words |
    |--------|-------------------|
    | Docx (mammoth raw) | ~2,650 |
    | Current index.html | ~1,977 |

    ### Content in Docx but NOT in HTML (add these)
    - [scene/passage]: [description]
    ...

    ### Harley Character Difference
    | Aspect | Current HTML | Docx Version |
    |--------|--------------|--------------|
    | Character name | Named "Harley", .harley CSS class | Anonymous — no name, italic only |
    | Voice attribution | <p class="harley"> / <span class="harley"> | Plain italic <em> commands |

    ### Decision Required
    [ ] Keep Harley named (HTML approach) — use .harley class in fragment
    [ ] Make anonymous (docx approach) — use <em> only, remove .harley class references
    [ ] Other (specify)

    ### Inline style= Violations in Current HTML (must be replaced)
    List all style= attributes found on Ch2 content (e.g., style="font-size: 1.05em") that must be replaced with .bold-grow-* classes in the final fragment.
    ```

    Be thorough and specific. This is the author's review document — each discrepancy must be individually listed so the author can make line-level decisions. Use inline code or blockquotes to show specific text samples where helpful.

    Flag the join point for the Ch2 docx content that must be added to the current HTML with a comment like: `<!-- NOTE: content from docx begins here — joins after "She just shrugged." -->`.
  </action>
  <verify>
    <automated>test -s chapters/DISCREPANCY.md && echo "DISCREPANCY.md exists and non-empty" || echo "FAIL: missing or empty"</automated>
  </verify>
  <done>chapters/DISCREPANCY.md exists, is non-empty, covers Ch1 word count comparison, lists all HTML-only scenes, lists all docx-only content, documents Harley character divergence, and has clear decision checkboxes for the author</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    A DISCREPANCY.md report comparing Ch1 and Ch2 docx sources against the current index.html. The report lists all content present in one version but not the other, including:
    - Ch1: HTML has ~1,400 more words than docx (Sisyphus/hairpin scene, Nico meeting, bold-grow SAME sequence)
    - Ch2: Docx has ~673 more words than HTML (Gianna couch ending, "The Ever-Absent Mercy of a Clock" poem)
    - Ch2: Harley is named in HTML but anonymous in docx
  </what-built>
  <how-to-verify>
    1. Open `chapters/DISCREPANCY.md` in any text editor or markdown viewer.
    2. Read the Chapter 1 section. Decide: should the final ch01.html use the docx text, the HTML text, or a merge of both? Check the appropriate box or reply with your decision.
    3. Read the Chapter 2 section. Decide: should Harley remain a named character (.harley class) or become anonymous (plain em tags)?
    4. Confirm you understand that docx-only Ch2 content (Gianna ending, poem) WILL be added to ch02.html regardless of the Harley decision.
    5. Reply below with your decisions:
       - Ch1 canonical version: "docx" | "html" | "merge: [describe strategy]"
       - Ch2 Harley: "named" | "anonymous"
  </how-to-verify>
  <resume-signal>Reply with: Ch1=[docx|html|merge] Ch2-Harley=[named|anonymous]</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Author ch01.html and ch02.html fragments</name>
  <files>chapters/ch01.html, chapters/ch02.html</files>
  <action>
    After the human-verify checkpoint decisions are received, create the two chapter HTML fragments.

    **ch01.html — based on user's canonical version decision:**

    If decision is "html" (keep current HTML content as canonical):
    - Extract the entire `<div class="chapter" id="chapter1">` block from `index.html`.
    - Reorganize into the fragment structure: sections get IDs `ch1-s1`, `ch1-s2`, `ch1-s3`, etc.
    - The `.poetic-text` div and `.staggered-indent` div already present in HTML should remain.
    - Section 3 (The Rehearsal poem from docx) — check ch01-raw.html; if the poem is present in the docx output, add it as a final section `ch1-s3` with `.poetic-text` wrapping the poem lines.
    - Remove the `<div></div>` spacer elements (they are empty divs, not meaningful structure).
    - Keep all `.bold-grow-*`, `.centered-text`, `.right-aligned`, `.alegreya-sans-sc-*` classes exactly as they are.

    If decision is "docx":
    - Use `chapters/raw/ch01-raw.html` as the base.
    - Apply formatting classes by recognizing patterns: date/location header → `.date-location`, poem lines after `<h3>` → `.poetic-text` wrapper, `<h2>` chapter title → add `class="chapter-title"`.
    - Add `<!-- TODO: verify formatting -->` comment on any passage where mammoth output is ambiguous.
    - The "The Rehearsal" poem becomes section `ch1-s3`.

    Regardless of decision:
    - File must be a standalone HTML fragment (no `<!DOCTYPE>`, no `<head>`, no `<body>`).
    - Outer element: `<div class="chapter" id="chapter1">`.
    - All section elements use IDs `ch1-s1`, `ch1-s2`, `ch1-s3` with `class="book-section"`.
    - No bare `section1`, `section2`, `section3` IDs.

    **ch02.html — based on user's Harley decision:**

    Base: Extract current Ch2 from `index.html` AND read `chapters/raw/ch02-raw.html` for the docx-only ending.

    Structure:
    - Section `ch2-s1`: Opening through the shower/book scene (existing HTML content).
    - Apply Harley decision: if "named" keep `.harley` class; if "anonymous" replace `<span class="harley">` and `<p class="harley">` with `<em>` tags.
    - Replace ALL inline `style=` attributes with equivalent CSS classes:
      - `style="font-size: 1.05em; font-weight: bold;"` → `class="bold-grow-1"` (use best judgment + `<!-- TODO: verify formatting -->`)
      - `style="font-size: 1.1em; font-weight: bold;"` → `class="bold-grow-2"`
      - `style="font-size: 1.2em; font-weight: bold;"` → `class="bold-grow-3"`
      - `style="font-size: 1.4em; font-weight: bold;"` → `class="bold-grow-4"`
      - `style="font-weight: 400; font-size: 1em;"` → plain `<p>` (no class)
      - `style="font-weight: 500; font-size: 1.2em;"` → `class="bold-grow-2"` with `<!-- TODO: verify formatting -->`
      - `style="font-weight: 700; font-size: 1.4em;"` → `class="bold-grow-4"` with `<!-- TODO: verify formatting -->`
    - Docx-only ending: Add the Gianna couch scene and "The Ever-Absent Mercy of a Clock" poem from `ch02-raw.html` after the existing HTML content ends. Mark the join point: `<!-- docx content begins here — verify join against original -->`.
    - The poem becomes: `<div class="poetic-text">` wrapping poem lines, preceded by an `<h3 class="chapter-title">` with the poem title.

    Both files: must pass `node tools/validate-chapters.cjs --check ch01` and `--check ch02`.
  </action>
  <verify>
    <automated>cd C:/Users/gspea/Book && node tools/validate-chapters.cjs --check ch01 ch02</automated>
  </verify>
  <done>Both ch01.html and ch02.html exist as valid fragments; validator passes for both; no inline style= attributes remain; section IDs use ch1-s* and ch2-s* format; ch02.html includes docx-only ending content</done>
</task>

</tasks>

<verification>
- `test -s chapters/DISCREPANCY.md` exits 0
- `node tools/validate-chapters.cjs --check ch01 ch02` exits 0
- Visually: ch01.html and ch02.html open in a browser (via local server) and render the chapter content with correct formatting
</verification>

<success_criteria>
- DISCREPANCY.md documents all Ch1 and Ch2 divergences with clear decision checkboxes
- ch01.html reflects the author's canonical version decision (docx or HTML)
- ch02.html merges existing HTML content with docx-only ending; Harley treatment per author decision
- All inline style= attributes eliminated from ch02.html; replaced with .bold-grow-* classes
- Both files are standalone HTML fragments with per-chapter section IDs
- Validator passes for both files
</success_criteria>

<output>
After completion, create `.planning/phases/01-content-migration/01-02-SUMMARY.md`
</output>
