import { ReaderAccountStub } from "@/components/reader/ReaderAccountStub";

export const dynamic = "force-dynamic";

export default function ReaderNotesPage(): React.ReactElement {
  return <ReaderAccountStub title="Notes" description="Your marginalia will live beside the prose, with conflict-aware sync as revisions evolve." />;
}
